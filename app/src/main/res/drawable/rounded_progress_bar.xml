<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@+id/roundedPrimaryColor">
        <shape
            android:shape="ring"
            android:useLevel="false"
            android:thicknessRatio="24">
            <solid android:color="@color/light_grey"/>
        </shape>
    </item>

    <item android:id="@+id/roundedSecondaryColor">
        <rotate
            android:fromDegrees="270"
            android:toDegrees="270">
            <shape
                android:shape="ring"
                android:useLevel="true"
                android:thicknessRatio="24">
                <!--                Gradinet -->
                <gradient android:angle="180" android:startColor="@color/purple_700" android:endColor="@color/dark_blue"/>
            </shape>
        </rotate>
    </item>
</layer-list>